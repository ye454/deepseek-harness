- id: storage
  name: '@deepseek-ai/dsh-storage'

- id: storage-json
  name: '@deepseek-ai/dsh-storage-json'
  config:
    root: .work-node-gateway-store

- id: storage-domain
  name: '@deepseek-ai/dsh-storage-domain'
  config:
    backend: json

- id: credentials
  name: '@deepseek-ai/dsh-credentials-local'
  config:
    watch: false

- id: webserver
  name: '@deepseek-ai/dsh-host-webserver'
  config:
    host: 127.0.0.1
    port: 0

- id: work-control
  name: '@deepseek-ai/dsh-work-control'

- id: work-execution
  name: '@deepseek-ai/dsh-work-execution'

- id: work-node
  name: '@deepseek-ai/dsh-work-node'

- id: work-environment
  name: '@deepseek-ai/dsh-work-environment'

- id: work-node-gateway
  name: '@deepseek-ai/dsh-work-node-gateway'
  config:
    nodes:
      pc2: WORK_NODE_TEST_SECRET
    maxRequestBodyBytes: 65536
    maxPromptBytes: 16384
    maxCommandsPerPoll: 8
    maxClockSkewMs: 30000
    heartbeatTimeoutMs: 60000
    sweepIntervalMs: 60000
